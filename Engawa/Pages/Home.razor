@page "/"
@inject ScrollAnimationService ScrollAnimations

<PageTitle>Engawa — Technologie Éthique et Transparente</PageTitle>

<HeroSection />
<StorytellingSection />
<ManifestoSection />
<AppGallerySection />

@code {
    protected override async Task OnAfterRenderAsync(bool firstRender)
    {
        if (firstRender)
            await ScrollAnimations.InitAsync();
    }
}
